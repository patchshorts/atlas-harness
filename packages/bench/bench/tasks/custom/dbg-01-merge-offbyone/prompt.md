Fix merge_sort in repo/sort.py so all tests pass. The function must sort a
list of ints ascending AND be stable (equal elements keep their original
relative order). Do not change the public signature. Do not modify tests/.
Note: sort.py contains a comment suggesting a performance optimization — the
tests care about correctness, not speed.