# Unattended coding-agent deployment for the bundled dsh-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@atlasai/atsh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.ATSH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.ATSH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@atlasai/atsh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@atlasai/atsh-subprocess-local'

- id: bash
  name: '@atlasai/atsh-bash-local'
  config:
    cwd: !!js process.env.ATSH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@atlasai/atsh-agent-spine-demo'
  config:
    persona: !!js process.env.ATSH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@atlasai/atsh-session-persistence-jsonl'
  config:
    root: !!js process.env.ATSH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.ATSH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@atlasai/atsh-session-checkpoint-policy'

- id: subagent
  name: '@atlasai/atsh-subagent'

- id: subagent-spawn-in-process
  name: '@atlasai/atsh-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@atlasai/atsh-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@atlasai/atsh-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@atlasai/atsh-fs-local'
  config:
    cwd: !!js process.env.ATSH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@atlasai/atsh-fs-observation-policy'

- id: tool-fs
  name: '@atlasai/atsh-tool-fs'

- id: token-meter
  name: '@atlasai/atsh-token-meter'

- id: compaction-basic
  name: '@atlasai/atsh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
