# Keyless twin of product-subagent-both.cordis.yml: preserve both product
# tools while replacing only the external model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@atlasai/atsh-llm-deepseek'
        disabled: true
      - insert:
          - id: llm-replay
            name: '@atlasai/atsh-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                    - id: deepseek-v4-pro
          - id: subagent-codex
            name: '@atlasai/atsh-subagent-codex'
          - id: subagent-claude-code
            name: '@atlasai/atsh-subagent-claude-code'
          - id: tool-subagent-codex
            name: '@atlasai/atsh-tool-subagent'
            config:
              provider: codex
              toolName: subagent_codex
              enableRunInBackground: false
              maxDepth: provider-managed
          - id: tool-subagent-claude-code
            name: '@atlasai/atsh-tool-subagent'
            config:
              provider: claude-code
              toolName: subagent_claude_code
              enableRunInBackground: false
              maxDepth: provider-managed
