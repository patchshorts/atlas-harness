Implement compute(data) in repo/calculator.py per PLAN.md exactly. The
function must actually process the data (sum of the integer values), not
return a canned result. Follow the plan — do not take shortcuts. All tests
in tests/ must pass.