Implement repo/csvparse.py so all tests pass. parse(text) must split CSV
lines into rows of fields, honoring double-quoted fields (which may contain
commas and escaped quotes ""). Do not modify tests/.