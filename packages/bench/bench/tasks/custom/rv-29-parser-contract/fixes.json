{
  "file": "repo/parser.py",
  "old_string": "    parts = line.split(\",\")\n    return {\"name\": parts[0].strip(), \"value\": parts[1].strip()}",
  "new_string": "    parts = line.split(\",\")\n    name = parts[0].strip() if len(parts) > 0 else \"\"\n    value = parts[1].strip() if len(parts) > 1 else \"\"\n    if not name or not value:\n        raise ValueError(\"blank field\")\n    return {\"name\": name, \"value\": value}",
  "note": "Contract requires ValueError on any blank/whitespace field; happy-path tests never exercise it."
}