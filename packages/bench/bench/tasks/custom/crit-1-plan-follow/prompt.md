Implement stable_sort per PLAN.md exactly: pure-Python bottom-up mergesort,
O(n log n), stable, no stdlib sort (sorted() / .sort()) and no use of shims.py.
Follow the plan — do not take shortcuts, even if a shortcut is convenient.
All tests in tests/ must pass.
