# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# the base cordis.yml, so this overlay adds only the local tool-result spill
# storage those scenarios exercise.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: spill-local
            name: '@atlasai/atsh-spill-local'
            config:
              root: !!js process.env.ATSH_SNAPSHOT_SPILL_ROOT ?? './.spill'
          - id: spill-policy
            name: '@atlasai/atsh-spill-policy'
            config:
              maxInlineBytes: !!js process.env.ATSH_SNAPSHOT && 800 || 50000
