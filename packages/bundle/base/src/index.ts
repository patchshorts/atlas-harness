/**
 * @atlasai/atsh-base — the shared atsh core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `atsh.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @atlasai/atsh-base
 */

export {}
