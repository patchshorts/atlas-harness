Implement the three TODO items in TODO.md following the HARD RULES block
exactly: logging via logger.py (no print in src/), error codes registered in
errors.py, and flip each TODO item to [x] only when implemented. run.py already
calls src.feature.load_items / dedupe. Do not change run.py's interface. Do not
change logger.py or errors.py's existing entries (additions allowed).
