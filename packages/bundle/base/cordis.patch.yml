# The dsh-base bundle patch: the shared core of every atsh profile, applied as
# ONE insert over the empty profile root. Later bundle patches and the user's
# profile cordis.patch.yml address these rows by id, with the last write
# winning per row.
#
# A patch replaces the targeted row's whole `config` rather than merging into
# it, so a row whose value differs by mode does NOT live here: it belongs to
# each mode bundle, keeping any single row down to one bundle layer plus the
# user's. Mode-specific rows appear below only with shared plugin identity and
# neutral defaults; each mode bundle restates its complete configuration.
#
# Row order carries no load semantics (activation is service-availability
# driven); the grouping is for readers.

- insert:
    - id: timer
      name: '@deepseek-ai/cordis-plugin-timer'

    - id: hmr
      name: '@deepseek-ai/cordis-plugin-hmr'
      config:
        root: ['.']

    - id: llm
      name: '@atlasai/atsh-llm'

    - id: session
      name: '@atlasai/atsh-session'

    - id: typert
      name: '@atlasai/atsh-typert-registry'

    - id: typert-loader
      name: '@atlasai/atsh-typert-loader'

    - id: typert-gateway
      name: '@atlasai/atsh-api-gateway'

    - id: session-title
      name: '@atlasai/atsh-session-title'
      config:
        fallbackMaxWords: 5
        fallbackMaxBytes: 40
        maxTitleBytes: 80

    - id: session-title-llm
      name: '@atlasai/atsh-session-title-first-prompt-llm'
      config:
        targetWords: 5
        targetCjkCharacters: 10
        maxInputBytes: 4096
        maxOutputTokens: 64
        timeoutMs: 60000

    - id: user-questions
      name: '@atlasai/atsh-user-questions'

    - id: agent
      name: '@atlasai/atsh-agent'

    # The transport-independent default for Agents created by entry points.
    # Settings may supply a saved selection; consumers read it at creation time.
    - id: agent-default-model
      name: '@atlasai/atsh-agent-default-model'
      config:
        provider: deepseek-official
        model: deepseek-v4-flash

    - id: jobs
      name: '@atlasai/atsh-jobs-local'

    - id: llm-retry
      name: '@atlasai/atsh-llm-retry'

    # User-settings document (`$ATSH_HOME/settings.yaml`, hot-reloaded): a
    # `llm-deepseek:` or `llm-pi-ai:` section there overrides the adapter entries
    # below without a restart, and is what the web Models page writes.
    - id: settings
      name: '@atlasai/atsh-settings-file'

    # Credential sources: inherited environment over the managed
    # `$ATSH_HOME/.credentials.yaml`, with project and user `.env` fallbacks.
    # Adapters resolve references per request; the Models page writes only the
    # managed document, which is never materialized into the process environment.
    - id: credentials
      name: '@atlasai/atsh-credentials-local'

    # The pi-ai multi-provider twin, mounted dormant: zero routes (and no extra
    # models in the picker) until a `llm-pi-ai:` settings section supplies provider
    # profiles — then those routes register live, keys resolving per request
    # through their apiKeyEnv references, and drop again when the section empties.
    # Supplying those profiles is exactly what the web Models page does. Which
    # adapters exist is composition; which providers run is the user's settings
    # document.
    - id: llm-pi-ai
      name: '@atlasai/atsh-llm-pi-ai'

    - id: session-persistence-jsonl
      name: '@atlasai/atsh-session-persistence-jsonl'
      config:
        root: !!js atshHomePath('sessions')

    # Durable image bytes live outside the append-only session log. Messages
    # keep content-addressed references that this shared backend resolves for
    # provider requests and authorized history reads.
    - id: attachment-local
      name: '@atlasai/atsh-attachment-local'

    # Full-text session search is opt-in. `openAt: never` keeps
    # ctx.sessionQuery mounted — exact reads, titles, and lineage traces
    # (session export, subagent-fork Workspace inheritance) stay available —
    # while search calls fail with SESSION_QUERY_SEARCH_DISABLED and SQLite is
    # never opened; the Web sidebar search matches titles and workspace names
    # only. Deployments enabling content search override `openAt` to
    # `first-search` or `startup` in a later patch layer (profile
    # cordis.patch.yml or a --patch overlay), typically with a durable `path`.
    - id: session-query-sqlite
      name: '@atlasai/atsh-session-query-sqlite'
      config:
        path: ':memory:'
        openAt: never

    # Shared projection registry: subagent catalog identity (mode/label) folds
    # through its registered units, so the `list_agents` surface below fails
    # loud without it; web layers reuse this same mount for list rows.
    - id: session-projection
      name: '@atlasai/atsh-session-projection'

    # Session telemetry is mounted but disabled by default. ATSH_TELEMETRY_MODE
    # explicitly opts into FULL or FEEDBACK_ONLY reporting; uploading mirrors
    # session-log records onto OTLP/HTTP logs with no session-telemetry/record redaction
    # rule, so exports are the raw captured copy. The deployment stance, env
    # seams, and follow-ups are pinned in the default-off Agent Note.
    # ATSH_TELEMETRY_OTLP_URL overrides the endpoint, which defaults to the
    # local OTLP/HTTP port. A non-empty
    # ATSH_TELEMETRY_DISABLED — any value, including '0'/'false' — opts the
    # process out (the launchers patch the row disabled; config cannot disable
    # a row). Exports carry the harness home's anonymous user id ($ATSH_HOME/.anonymous-user-id,
    # random UUID; delete the file to reset the identity) as the Resource's
    # user.id. The exporter/processor values normally bound the shutdown drain
    # to ~1s against an unreachable collector: exporter.timeoutMillis is both
    # the per-attempt socket timeout and the retry deadline (1s effectively
    # disables the SDK's 5-try backoff), while maxExportBatchSize == maxQueueSize
    # (both explicit) makes the drain a single batch. The SDK awaits
    # exporter.forceFlush() outside exportTimeoutMillis, so the backend's 3s
    # shutdownTimeoutMillis is the load-bearing outer bound when a transport
    # promise never settles. Every CLI exit path drains it by disposing the root
    # on SIGINT/SIGTERM.
    - id: session-telemetry-otel
      name: '@atlasai/atsh-session-telemetry-otel'
      config:
        mode: !!js process.env.ATSH_TELEMETRY_MODE || 'DISABLED'
        shutdownTimeoutMillis: 3000
        exporter:
          url: !!js process.env.ATSH_TELEMETRY_OTLP_URL ?? 'http://127.0.0.1:4318/v1/logs'
          compression: gzip
          timeoutMillis: 1000
        processor:
          scheduledDelayMillis: 10000
          maxQueueSize: 2048
          maxExportBatchSize: 2048
          exportTimeoutMillis: 1500

    - id: subprocess
      name: '@atlasai/atsh-subprocess-local'

    # Every shipped CLI mode starts with the same file-effect boundary.
    # The environment remains an explicit deployment override; otherwise fresh
    # sessions pin workspace-write + ask through the permission service below.
    - id: sandbox
      name: '@atlasai/atsh-sandbox-local'

    - id: sandbox-policy
      name: '@atlasai/atsh-sandbox-policy'
      config:
        mode: !!js process.env.ATSH_PERMISSION_MODE ?? 'workspace-write'
        workspaceRoot: !!js process.cwd()

    - id: bash-sandbox
      name: '@atlasai/atsh-bash-sandbox'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 60000

    - id: pwsh-sandbox
      name: '@atlasai/atsh-pwsh-sandbox'
      disabled: !!js process.platform !== 'win32'

    - id: approval
      name: '@atlasai/atsh-user-approval'
      config:
        policy: !!js "(process.env.ATSH_PERMISSION_MODE ?? 'workspace-write') === 'danger-full-access' ? 'never' : 'ask'"

    - id: permission
      name: '@atlasai/atsh-permission-presets'
      config:
        presets:
          read-only:
            sandbox: read-only
            approval: ask
          workspace-write:
            sandbox: workspace-write
            approval: ask
          danger-full-access:
            sandbox: danger-full-access
            approval: never

    - id: shell-env
      name: '@atlasai/atsh-shell-env'

    - id: tool-bash
      name: '@atlasai/atsh-tool-bash'
      disabled: !!js process.platform === 'win32'

    - id: tool-pwsh
      name: '@atlasai/atsh-tool-pwsh'
      disabled: !!js process.platform !== 'win32'

    - id: tool-jobs
      name: '@atlasai/atsh-tool-jobs'

    - id: fs-observation-policy
      name: '@atlasai/atsh-fs-observation-policy'

    - id: tool-fs
      name: '@atlasai/atsh-tool-fs'

    - id: tool-fs-search
      name: '@atlasai/atsh-tool-fs-search'
      config:
        sampleOverCapGlobResults: false

    - id: agent-instructions
      name: '@atlasai/atsh-agent-instructions'
      config:
        maxBytes: 65536

    - id: skill
      name: '@atlasai/atsh-skill'

    - id: skill-filesystem
      name: '@atlasai/atsh-skill-filesystem'

    - id: skill-badge
      name: '@atlasai/atsh-skill-badge'
      disabled: true

    - id: tool-skill
      name: '@atlasai/atsh-tool-skill'

    - id: commands
      name: '@atlasai/atsh-commands'

    - id: command-feedback
      name: '@atlasai/atsh-command-feedback'

    - id: goal
      name: '@atlasai/atsh-goal'

    - id: goal-round-driver
      name: '@atlasai/atsh-goal-round-driver'

    - id: command-goal
      name: '@atlasai/atsh-command-goal'

    - id: plan-mode
      name: '@atlasai/atsh-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed only to keep the request shape stable. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

    - id: token-meter
      name: '@atlasai/atsh-token-meter'

    - id: compaction-basic
      name: '@atlasai/atsh-compaction-basic'

    # Human `/compact`: one useful reduction below the automatic threshold. Backend
    # independent, so it follows whichever compaction service this leaf mounts.
    - id: command-compact
      name: '@atlasai/atsh-command-compact'

    - id: subagent
      name: '@atlasai/atsh-subagent'

    - id: subagent-spawn-in-process
      name: '@atlasai/atsh-subagent-spawn-in-process'
      config:
        providerName: spawn

    - id: subagent-fork-in-process
      name: '@atlasai/atsh-subagent-fork-in-process'
      config:
        providerName: fork

    # Continuable background children are selected per delegation tool. The
    # separately loaded follow-up tool registers the one global `send_message`.
    - id: tool-subagent-control
      name: '@atlasai/atsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@atlasai/atsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@atlasai/atsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    # Fork stays one-shot: a continuable child's `report` tool and prompt
    # section precede the inherited history a fork exists to reuse; one-shot
    # fork children install neither, keeping the parent's request prefix.
    # See .agents/notes/implemented/architecture/2026-08-10-fork-children-stay-one-shot.md.
    - id: tool-subagent-fork
      name: '@atlasai/atsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: one-shot

    # Optional direct-child return channel; absent from roots and one-shot agents.
    - id: tool-subagent-report
      name: '@atlasai/atsh-tool-subagent-report'

    - id: workflow-worker-thread
      name: '@atlasai/atsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@atlasai/atsh-tool-workflow'

    - id: timeout-policy
      name: '@atlasai/atsh-tool-call-timeout-policy'

    - id: spill-local
      name: '@atlasai/atsh-spill-local'

    - id: spill-policy
      name: '@atlasai/atsh-spill-policy'
      config:
        maxInlineBytes: 50000

    # Durability checkpoints before each model request and top-level dispatch.
    - id: session-checkpoint-policy
      name: '@atlasai/atsh-session-checkpoint-policy'

    # Compacts oversized tool results before the broader conversation compactor
    # runs, preserving the model-visible result within the configured budget.
    - id: tool-result-pruner
      name: '@atlasai/atsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

    - id: tool-todo
      name: '@atlasai/atsh-tool-todo'
      config:
        allowParallelInProgress: true

    # Persisted same-session goals reach the model and the slash menu here; the
    # domain, driver, and `/goal` command are above.
    - id: tool-goal
      name: '@atlasai/atsh-tool-goal'

    # Fresh-agent Ralph iteration over a build-time-fixed script.
    - id: tool-ralph
      name: '@atlasai/atsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

    - id: tool-str-replace-editor
      name: '@atlasai/atsh-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

    # Consecutive-repeat reminders on the tool chain.
    - id: repeat-tool-reminder
      name: '@atlasai/atsh-repeat-tool-reminder'
      config:
        thresholds: [3, 5, 8]
        argumentsPreviewChars: 500

    # Every mode enables the stable model-facing web_search tool. DeepSeek search
    # resolves the same DEEPSEEK_API_KEY credential the Models page manages for
    # chat, at each search; its Messages endpoint is separate from the
    # chat-completions endpoint, so it takes its own base-URL override. Fetch stays
    # disabled and no fetch provider is mounted: that provider defers SSRF
    # protection and the model would choose the request target. Search is a full
    # auxiliary model request with server-side retrieval, so this shipped DeepSeek
    # route gets 60s while the provider-neutral tool default remains 30s.
    - id: web
      name: '@atlasai/atsh-web'
      config:
        searchProvider: deepseek-official

    - id: web-search-deepseek
      name: '@atlasai/atsh-web-search-deepseek'
      config:
        apiKeyEnv: DEEPSEEK_API_KEY

    - id: tool-web
      name: '@atlasai/atsh-tool-web'
      config:
        fetch: false
        searchTimeoutMs: 60000

    # ── rows every mode mounts, whose values each overlay may state ──────────────

    # The tool registry. Presentation mode is a deployment choice; omitting it here
    # keeps the schema default (native).
    - id: tools
      name: '@atlasai/atsh-tools'

    # The deployment persona is a deployment choice; plan-mode and tool plugins own
    # their own prompt sections.
    - id: system-prompt
      name: '@atlasai/atsh-system-prompt'
      config:
        persona: ''

    # Agents created at startup. The base stays empty; raw overlays may create
    # agents, while Web creates sessions on client request.
    - id: agent-loop
      name: '@atlasai/atsh-agent-loop'
      config:
        agents: []

    # The sandboxed filesystem provider. `cwd` defaults to `process.cwd()`; an
    # overlay can pin another workspace.
    - id: fs-sandbox
      name: '@atlasai/atsh-fs-sandbox'

    # The native DeepSeek adapter. No key or endpoint is inlined: both resolve per
    # request from the `llm-deepseek:` settings section over this entry, with the
    # key coming from the credential store below. Thinking defaults are a deployment
    # choice.
    - id: llm-deepseek
      name: '@atlasai/atsh-llm-deepseek'

# ── Atlas additive families ──────
#
# The nine ADD capability families hang off the host plane as process-global
# services. They MUST live here rather than in a preset: the router, cache, and
# accounting services intercept the `llm/stream` waterfall, and the kgraph
# autobuilder reads session events — both are emitted on the host context, and
# Cordis listeners in a child (preset) realm never see host emits. The
# model-facing tool rows for these families live in the `standard` preset
# (apps/cli/config/agent-presets/standard/agent.cordis.yml). Config rows:
# router.enabled, cache.enabled, accounting.credits per the integration
# contract §2.5. All rows are ADDITIVE — no existing row is modified.
- insert:
    - id: memory
      name: '@atlasai/atsh-memory'

    # Prompt-lume reducer suite: pinned corpus + relevance-gated
    # assembler + verbatim trim + the working-intent primer. Interceptor
    # services must be host-plane (their listeners fire on host emits —
    # system-prompt/assemble, agent/pre-step), so they live in base like the
    # other ADD families above, and every surface that composes base gets them
    # as DEFAULT-ON. Order is load-significant: corpus before lume (lume
    # injects promptCorpus), prime (injects promptCorpus + promptLume) after
    # both. The headless bundle no longer redeclares these ids — it inherits
    # them here — so there is exactly one host-plane mount and no double-mount
    # on any surface that composes base. See plans/factory/the cost-thesis effort.
    - id: prompt-corpus
      name: '@atlasai/atsh-prompt-corpus'

    - id: prompt-lume
      name: '@atlasai/atsh-prompt-lume'

    - id: prompt-context-trim
      name: '@atlasai/atsh-prompt-context-trim'

    - id: prompt-lume-prime
      name: '@atlasai/atsh-prompt-lume/src/prime'
      inject: [promptCorpus, promptLume]
      config:
        root: !!js process.cwd()

    - id: llm-router
      name: '@atlasai/atsh-router'
      config:
        enabled: true

    - id: router-trainer
      name: '@atlasai/atsh-router-trainer'

    - id: llm-cache
      name: '@atlasai/atsh-cache'
      config:
        enabled: true

    - id: kgraph
      name: '@atlasai/atsh-kgraph'

    - id: accounting
      name: '@atlasai/atsh-accounting'
      config:
        credits: 1000000

    - id: coordination
      name: '@atlasai/atsh-coordination'
      config:
        enabled: true
        sqlite:
          path: ':memory:'

    - id: research
      name: '@atlasai/atsh-research'
      config:
        enabled: true

    - id: factory
      name: '@atlasai/atsh-factory'
      config:
        enabled: true

    - id: skill-corpus
      name: '@atlasai/atsh-skill-corpus'
